const authRouter = require("./auth");
const usersRouter = require("./users");
const productsRouter = require("./products");

module.exports = {
      authRouter,
      usersRouter,
      productsRouter,
};
